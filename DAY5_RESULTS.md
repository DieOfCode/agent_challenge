# Day 5 Results: Model Versions

## Prompt
Сравни HTTP/1.1, HTTP/2 и HTTP/3 для мобильного API. Дай 3 отличия и 1 рекомендацию. Обязательно упомяни: multiplexing, head-of-line blocking, QUIC.

## Models

### WEAK model: `openai/gpt-4o-mini`
- Latency: `598ms`
- Tokens: `prompt=57 completion=350 total=407`
- Cost: `$0.000219`
- Quality score: `45/100`
- OpenRouter: https://openrouter.ai/openai/gpt-4o-mini

Answer:
```text
Сравнение HTTP/1.1, HTTP/2 и HTTP/3 для мобильного API:

### Отличия:

1. **Multiplexing**:
   - **HTTP/1.1**: Поддерживает только одно соединение на запрос, что означает, что каждый запрос должен ждать завершения предыдущего. Это приводит к необходимости использования нескольких соединений для параллельной загрузки ресурсов.
   - **HTTP/2**: Вводит поддержку мультиплексирования, что позволяет отправлять несколько запросов и получать ответы одновременно по одному соединению. Это значительно улучшает производительность и уменьшает задержки.
   - **HTTP/3**: Также поддерживает мультиплексирование, но использует QUIC в качестве транспортного протокола, что дополнительно улучшает скорость и надежность соединений.

2. **Head-of-Line Blocking**:
   - **HTTP/1.1**: Страдает от проблемы "head-of-line blocking", когда один медленный запрос блокирует все последующие запросы в очереди.
   - **HTTP/2**: Устраняет эту проблему благодаря мультиплексированию, позволяя запросам обрабатываться независимо друг от друга.
   - **HTTP/3**: Продолжает избегать "head-of-line blocking" за счет использования QUIC, который работает на уровне UDP и позволяет обрабатывать потерю пакетов более эффективно.

3. **QUIC**:
   - **HTTP/1.1 и HTTP/2**: Оба используют TCP в качестве транспортного протокола, что может привести к задержкам из-за установления соединения и управления потоком.
   - **HTTP/3**: Использует QUIC
```

### MID model: `openai/gpt-4.1-mini-2025-04-14`
- Latency: `410ms`
- Tokens: `prompt=57 completion=350 total=407`
- Cost: `$0.000583`
- Quality score: `65/100`
- OpenRouter: https://openrouter.ai/openai/gpt-4.1-mini

Answer:
```text
Вот сравнение HTTP/1.1, HTTP/2 и HTTP/3 для мобильного API с акцентом на multiplexing, head-of-line blocking и QUIC:

### 3 отличия

1. **Multiplexing**  
- **HTTP/1.1:** Не поддерживает multiplexing. Каждый запрос требует отдельного TCP-соединения или использует pipelining с ограничениями, что снижает эффективность.  
- **HTTP/2:** Поддерживает multiplexing — несколько запросов и ответов могут передаваться одновременно по одному TCP-соединению, что уменьшает задержки и повышает производительность.  
- **HTTP/3:** Также поддерживает multiplexing, но уже поверх протокола QUIC, что дополнительно улучшает скорость и надежность передачи.

2. **Head-of-line blocking (HOL blocking)**  
- **HTTP/1.1:** HOL blocking возникает из-за последовательной обработки запросов в одном соединении.  
- **HTTP/2:** HOL blocking на уровне TCP-соединения сохраняется — если один пакет теряется, все потоки ждут повторной передачи.  
- **HTTP/3:** HOL blocking значительно снижен благодаря использованию QUIC, который работает поверх UDP и реализует независимую доставку для каждого потока.

3. **Используемый транспортный протокол (QUIC)**  
- **HTTP/1.1 и HTTP/2:** Работают поверх TCP.  
- **HTTP/3:** Использует QUIC — транспортный протокол поверх UDP, который обеспечивает более быструю установку соединения, улучшенную устойчивость к потерям пакетов и встроенное шифрование.

### Рекоменда
```

### STRONG model: `openai/gpt-4.1-2025-04-14`
- Latency: `449ms`
- Tokens: `prompt=57 completion=350 total=407`
- Cost: `$0.002914`
- Quality score: `100/100`
- OpenRouter: https://openrouter.ai/openai/gpt-4.1

Answer:
```text
Вот сравнение HTTP/1.1, HTTP/2 и HTTP/3 для мобильного API:

**1. Multiplexing (Мультиплексирование):**
- **HTTP/1.1:** Не поддерживает мультиплексирование — каждое соединение обслуживает только один запрос одновременно. Для параллельных запросов открываются несколько TCP-соединений.
- **HTTP/2:** Поддерживает мультиплексирование — несколько запросов и ответов могут передаваться одновременно по одному TCP-соединению.
- **HTTP/3:** Также поддерживает мультиплексирование, но уже поверх протокола QUIC (а не TCP).

**2. Head-of-line blocking (Блокировка по голове очереди):**
- **HTTP/1.1:** Сильно страдает от head-of-line blocking — если один запрос задерживается, остальные ждут.
- **HTTP/2:** Устраняет head-of-line blocking на уровне HTTP, но не на уровне TCP: если теряется пакет, все потоки ждут его восстановления.
- **HTTP/3:** Благодаря использованию QUIC (работает поверх UDP), head-of-line blocking устраняется и на транспортном уровне — потеря пакета влияет только на соответствующий поток.

**3. QUIC:**
- **HTTP/1.1 и HTTP/2:** Работают поверх TCP.
- **HTTP/3:** Работает поверх QUIC (UDP), что обеспечивает более быструю установку соединения, встроенное шифрование и лучшую работу в условиях нестабильных мобильных сетей.

---

**Рекомендация:**  
Для мобильного API рекомендуется использовать **HTTP/3**, так как он
```

## Comparison
- Quality winner: `STRONG (openai/gpt-4.1-2025-04-14)`
- Speed winner: `MID (openai/gpt-4.1-mini-2025-04-14)`
- Efficiency winner: `WEAK (openai/gpt-4o-mini)`

Short conclusion: weak models are often cheaper/faster, strong models tend to give better quality, and mid-tier models are a practical balance.
